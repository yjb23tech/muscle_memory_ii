import logo from "../images/react-logo.png"

function Header() {
    return (
        <div>
            <nav>
                <img src={logo} width="60px" />
            </nav>
        </div>
    );
};

export default Header;
